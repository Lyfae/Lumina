.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# Distribution / packaging artifacts (DMG, staged app bundles, build logs)
dist/
*.dmg
dmg-staging/
build.log
*.log

# SwiftPM resolved dependencies (if using)
Package.resolved

# macOS / Xcode noise
*.xcuserstate
*.xcscmblueprint
.swiftpm/xcode/package.xcworkspace/xcuserdata/

# Local tooling / notes used while building Lumina (kept on disk, not in the public repo)
docs/
scripts/
Tests/
/build
/run

# Local test media or user data (never commit personal wallpapers)
# UserMedia/
# Thumbnails/  (if ever cached locally outside build)

# Temporary assets and archives (keep bundled Resources/ icons tracked)
*.zip
/dist/**/*.mov
/dist/**/*.mp4
/UserMedia/
