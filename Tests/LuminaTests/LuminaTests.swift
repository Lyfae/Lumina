import XCTest
@testable import Lumina

final class LuminaTests: XCTestCase {
    func testPlaceholder() throws {
        // MVP placeholder. Real PowerManager / renderer tests live in their own files.
        XCTAssertTrue(true)
    }
}
